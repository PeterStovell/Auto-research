dataloader:
  input_path: gs://demand-vision/quark/jobs/eugene/quark-multi-build-5vh69-08-24-2025/build-control-1.parquet
  query: y_volume_m0 >= 0 and y_volume_m2 >= 0
  train_end_date: '2023-07-01'
  val_end_date: '2024-01-01'
  columns:
    date: Date
    sequence: sequence
    categoricals:
      - Material
      - Plant
      - month_of_year
      - Refinery
    numericals:
      - y_volume_m0
    targets:
      - y_volume_m2
    scaling: [Material]

seed: 13
